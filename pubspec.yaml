name: waveui
description: "An enterprise-class package of Flutter components for mobile applications."
version: 1.0.0
homepage:

environment:
  sdk: ^3.7.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  better_lints: ^1.0.4


flutter:
